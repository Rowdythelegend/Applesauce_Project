import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bambanatech.applesauce',
  appName: "Kristel's Genius Hub",
  webDir: 'dist'
};

export default config;
